/// Approximate rendered height of [OrderBottomSheet] — used by the Lottie
/// stage views to reserve bottom space so their content doesn't get hidden
/// behind the sheet. Update this if the sheet's content height changes.
const double kOrderSheetApproxHeight = 340;
